from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Provide an internal API that could be used to assess the quality of a translation item (pair source / translation).',
    author='NLP Team',
    license='',
)
